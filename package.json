{
  "name": "ng-require",
  "version": "1.0.0",
  "description": "Angular 1.x with require js",
  "main": "index.js",
  "scripts": {
    "start": "npm run lite",
    "lite": "lite-server",
    "prebuild": "npm run clean -s",
    "//": "build is to minify the code into one JS file",
    "build": "node node_modules/requirejs/bin/r.js -o build.js",
    "clean": "rimraf ./dist/**/*.js"
  },
  "author": "lenient",
  "license": "ISC",
  "dependencies": {
    "angular": "^1.5.5",
    "angular-ui-router": "^0.3.0",
    "requirejs": "^2.2.0"
  },
  "devDependencies": {
    "lite-server": "^2.2.0",
    "ng-annotate": "^1.2.1",
    "rimraf": "^2.5.2"
  }
}
